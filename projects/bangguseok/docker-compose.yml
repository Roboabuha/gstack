version: '3.8'

services:
  bangguseok-app:
    build: .
    container_name: bangguseok-app
    restart: always
    ports:
      - "3777:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    
    # --- [Traefik 연동 보류 (도메인 구매 후 주석 해제)] ---
    # networks:
    #   - root_default
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.bangguseok.rule=Host(`photo.yourdomain.com`)"
    #   - "traefik.http.routers.bangguseok.entrypoints=websecure"
    #   - "traefik.http.routers.bangguseok.tls.certresolver=letsencrypt"
    #   - "traefik.http.services.bangguseok.loadbalancer.server.port=3000"

# networks:
#   root_default:
#     external: true
